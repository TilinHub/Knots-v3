"""Modulo 9: variables (STUB declarado, sin logica todavia).

Empaqueta y desempaqueta variables para optimizacion no lineal:
x = (c_1x, c_1y, ..., c_Nx, c_Ny, theta_1, ..., theta_M).

El orden de las etiquetas debe ser el mismo en pack_variables, unpack_variables,
assemble_g_red y todos los reportes.

Se implementa en su punto correspondiente (Modulo 9).
"""

_TODO = "variables: pendiente de implementar (Modulo 9)"


def pack_variables(cs):
    raise NotImplementedError(_TODO)


def unpack_variables(x, template):
    raise NotImplementedError(_TODO)


def variable_report(cs):
    raise NotImplementedError(_TODO)
