"""Modulo 11: optimise (STUB declarado, sin logica todavia).

Minimiza la longitud para un tipo cs fijo. No se debe escribir antes de que
funcionen length, knot_diagram, crossings, rolling, reduced, descent, variables y
validation. Contactos prescritos son igualdades; no contactos son desigualdades.

Se implementa en su punto correspondiente (Modulo 11).
"""

_TODO = "optimise: pendiente de implementar (Modulo 11)"


def objective_length(x, template):
    raise NotImplementedError(_TODO)


def contact_constraints(x, template, contacts):
    raise NotImplementedError(_TODO)


def clearance_constraints(x, template, contacts):
    raise NotImplementedError(_TODO)


def minimise_length(template, contacts):
    raise NotImplementedError(_TODO)


def optimisation_report(result, template, contacts):
    raise NotImplementedError(_TODO)
